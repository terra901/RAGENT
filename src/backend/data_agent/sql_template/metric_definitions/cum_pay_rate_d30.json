{
  "metric_id": "cum_pay_rate_d30",
  "name": "D30 累计付费率",
  "default_unit": "ratio",
  "aggregation": "ratio",
  "expr_by_table": {
    "ads_material_daily": "SUM(cumulative_pay_user_d30) / NULLIF(SUM(game_new_user_cnt), 0)",
    "ads_cohort_pivot": "SUM(cumulative_pay_user_d30) / NULLIF(SUM(enter_user_cnt), 0)",
    "dws_v4_cohort_user": "SUM(cum_pay_user_cnt) / NULLIF(SUM(d0_enter_user_cnt), 0)"
  },
  "recommended_filters": {
    "dws_v4_cohort_user": "day_n = 30"
  },
  "description": "「D30 累计付费率」指标，默认单位为 ratio，聚合方式为 ratio。单表表达式适用于 ads_material_daily、ads_cohort_pivot、dws_v4_cohort_user。ratio 类指标必须先聚合分子和分母，再相除，避免平均比率造成口径偏差。",
  "business_context": "用于回答与「D30 累计付费率」相关的排序、趋势、对比和汇总问题。",
  "query_guidance": "生成 SQL 时必须根据当前 source_table 或 template_name 选择匹配表达式；输出别名固定使用 metric_id「cum_pay_rate_d30」。"
}
