{
  "metric_id": "cpm",
  "name": "CPM",
  "default_unit": "yuan",
  "aggregation": "ratio",
  "expr_by_table": {
    "dws_v4_ad_cost_daily": "SUM(cost_amt) / 100 / NULLIF(SUM(impression_cnt), 0) * 1000"
  },
  "description": "「CPM」指标，默认单位为 yuan，聚合方式为 ratio。单表表达式适用于 dws_v4_ad_cost_daily。ratio 类指标必须先聚合分子和分母，再相除，避免平均比率造成口径偏差。",
  "business_context": "用于回答与「CPM」相关的排序、趋势、对比和汇总问题。",
  "query_guidance": "生成 SQL 时必须根据当前 source_table 或 template_name 选择匹配表达式；输出别名固定使用 metric_id「cpm」。"
}
