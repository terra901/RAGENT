{
  "metric_id": "cvr",
  "name": "转化率 CVR",
  "default_unit": "ratio",
  "aggregation": "ratio",
  "expr_by_table": {
    "dws_v4_ad_cost_daily": "SUM(platform_convert_cnt) / NULLIF(SUM(click_cnt), 0)"
  },
  "description": "「转化率 CVR」指标，默认单位为 ratio，聚合方式为 ratio。单表表达式适用于 dws_v4_ad_cost_daily。ratio 类指标必须先聚合分子和分母，再相除，避免平均比率造成口径偏差。",
  "business_context": "用于衡量点击到平台转化效率。",
  "query_guidance": "生成 SQL 时必须根据当前 source_table 或 template_name 选择匹配表达式；输出别名固定使用 metric_id「cvr」。"
}
