{
  "dimension_id": "channel",
  "name": "渠道商",
  "field_by_table": {
    "*": "cch_id"
  },
  "description": "用于按「渠道商」筛选、分组、排序或下钻的分类维度。字段映射覆盖 1 类表映射；生成 SQL 时必须选择当前 source_table 对应字段，不要跨表复用不属于该表的字段。",
  "query_guidance": "当用户提到「渠道商」或其业务同义词时优先召回该维度；适用表范围：所有包含通用字段的业务表。"
}
