# tasks

Celery 任务目录，放异步任务入口。任务函数应尽量薄，只负责领取任务、调用服务层、回写状态。

## 文件树

```text
tasks/
├── __init__.py      # 包标记
└── ask_tasks.py     # 问数队列任务占位实现
```

## 设计说明

- 任务命名使用 `ragent.*` 前缀，避免和其他项目冲突。
- 当前 `/api/jobs/ask` 已能入队，后续可以把流式问数链路迁移到任务内执行。
- 任务不得直接依赖前端状态。
