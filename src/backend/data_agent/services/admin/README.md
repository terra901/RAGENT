# services/admin

后台管理服务扩展目录。当前模型管理逻辑较轻，直接由控制器调用 `admin/` 仓储；当后台规则变复杂时，将编排服务放到这里。

## 文件树

```text
services/admin/
└── README.md    # 后台服务扩展说明
```

## 设计说明

- 适合放跨仓储事务、批量导入导出、审计写入等后台服务。
- 不保存 HTTP Request，保持可被脚本和任务复用。
