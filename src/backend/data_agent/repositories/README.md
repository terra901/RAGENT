# repositories

仓储扩展目录。当前已有仓储按领域放在 `storage/`、`admin/`、`runtime/` 中；这里保留为后续跨领域仓储或插件仓储的落点。

## 文件树

```text
repositories/
└── README.md    # 仓储边界说明
```

## 设计说明

- 新仓储如果只服务单一领域，优先放入对应领域目录。
- 跨多个领域复用的仓储可以放到这里，并通过接口注入给服务层。
- 不在控制器里直接创建仓储，统一由 `api/bootstrap.py` 装配。
